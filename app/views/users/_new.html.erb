<%= bootstrap_form_for :user, url:signup_path(:user), html: { method: :post } do |f| %>
  <%= f.text_field :name, placeholder: 'Name' %>
  <%= f.email_field :email, placeholder: 'Email', id:'signupemail' %>
  <%= f.password_field :password, placeholder: 'Password (min 8 characters)', id: 'signuppass' %>
  <%= f.submit 'SignUp' %>
<% end %>
